For a simple script controlling LEDs with toggle buttons, see testScriptR.sh.
testScriptR.sh takes four arguments as gpio pins- if none are entered, the
program will inform you and close without issue.


For a simple terminal-based etch-a-sketch game, see etchsketch.c.
For the same game controlled by toggle switches, see etchsketchR.c
